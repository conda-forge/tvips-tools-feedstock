--- tvips/test/test_md5.cmake.orig
+++ tvips/test/test_md5.cmake
@@ -9,7 +9,7 @@
 # without any warranty.
 
 execute_process(
-  COMMAND "${CMD}" -o "t_###.img" "${FILE}"
+  COMMAND "${CMD}" -o "t_###.img" -z "EST5EDT" "${FILE}"
   RESULT_VARIABLE status)
 if(NOT "${status}" EQUAL "0")
   message(SEND_ERROR "Runtime error")
